.git
misc/
test/
# The licensed HSL library is bind-mounted at /data/lib at runtime (see
# docker-compose.yml), so copying it into the image only bloats it -- ~473 MB of
# binaries that are never read from /app/hsl, plus the build-context upload.
hsl/
PLAN.md
README.md
Dockerfile
fly.toml
docker-compose.yml
Caddyfile
.env
.env.example
DEPLOY_HOSTINGER.md
*.log
nohup.out
.DS_Store
